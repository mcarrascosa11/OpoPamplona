{
  "name": "oposicion-arquitecto-pamplona",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "auditar:preguntas": "node scripts/auditar_preguntas.cjs",
    "auditar:fuentes": "node scripts/auditar_fuentes.cjs",
    "check": "npm run auditar:preguntas && npm run build",
    "check:local": "npm run auditar:preguntas && npm run auditar:fuentes && npm run build"
  },
  "dependencies": {
    "@google/genai": "^1.24.0",
    "@supabase/supabase-js": "^2.45.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
